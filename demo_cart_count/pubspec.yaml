name: demo_cart_count
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  json_annotation: ^3.0.1
  get_it: ^7.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:


  uses-material-design: true
